version: '3'

services:
  master:
    image: marcel1199/rtl_433-mqtt_rfraw_support:alpine-master
    build:
      context: ../..
      args:
        - alpineVersion=latest
        - rtl433GitRevision=mqtt_rfraw_support
